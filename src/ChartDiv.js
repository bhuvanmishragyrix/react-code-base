import React from 'react';

const chartDiv = (props) => {
    return (
        <div className="col-lg-4">
            <div className={props.chartId}>

            </div>
        </div>
    );
};

export default chartDiv;